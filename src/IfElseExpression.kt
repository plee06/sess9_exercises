/**
 * And as we already know, Kotlin if-else expressions exist!
 *
 * i.e. we can assign something from an if-else to a val / var
 *
 * Because it is an expression it can be evaluated!
 *
 *
 * */




// Test out blank Strings
fun isBlankString(s: String) = if (s.isEmpty()) true else false

